[package]
name = "agent-guard"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]
doctest = false

[dependencies]
soroban-sdk = { version = "27.0.0" }

[dev-dependencies]
soroban-sdk = { version = "27.0.0", features = ["testutils"] }

[lints]
workspace = true
