[workspace]
resolver = "2"
members = ["contracts/*"]

[profile.release]
opt-level = "z"        # Optimize for binary size — critical for Wasm contracts
debug = 0
strip = "symbols"
debug-assertions = false
panic = "abort"
codegen-units = 1
lto = true
